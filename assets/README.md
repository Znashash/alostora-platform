# Shared Source Assets

Working/source assets that live outside the shipped theme (raw exports, mockups, originals).

Production-ready, optimized assets used by the theme are under
[`../theme/assets/`](../theme/assets/) (`images/`, `fonts/`, compiled `css/`, `js/`). The brand
logo source is at `../theme/assets/images/logo.svg` (mirrored in `../references/logo.svg`).
